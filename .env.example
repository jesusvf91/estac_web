# Copia a .env.local para desarrollo.

# Access Key de https://web3forms.com (llega a tu email al crearla).
# En producción: GitHub → Settings → Secrets → Actions → WEB3FORMS_ACCESS_KEY
# (el workflow la inyecta como NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY en el build).
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=
